import * as types from './actionTypes';

const INITIAL_STATE = {};

const success = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default success;
